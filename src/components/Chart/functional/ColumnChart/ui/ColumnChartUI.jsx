import React from 'react';

const ColumnChartUI = ({height, classes}) => {
    return (
        <div style={{height: height}} className={classes['chart-column']}></div>
    )
}

export default ColumnChartUI
